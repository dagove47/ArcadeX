@model arcadeX.Entidades.Usuario

@{
    Layout = "~/Views/Shared/_LayoutInitial.cshtml";
    ViewBag.Title = "RegistroUsuario";
}

<main class="">
    <section class="regLog col">
        <h1 class="regLog-Title">Registrarse</h1>

        @if (ViewBag.msj != null)
        {
            <div class="alert alert-info" role="alert" style="text-align:center">
                @ViewBag.msj
            </div>
        }

        @using (Html.BeginForm("RegistroUsuario", "Store"))
        {
            <div class="input-group">
                @Html.TextBoxFor(m => m.Identificacion, new { @type = "text", @class = "form-control", @placeholder = "Cedula" })
            </div>
            <div class="input-group">
                @Html.TextBoxFor(m => m.Nombre, new { @type = "text", @class = "form-control", @placeholder = "Nombre" })
            </div>
            <div class="input-group">
                @Html.TextBoxFor(m => m.Email, new { @type = "email", @class = "form-control", @placeholder = "Correo Electrónico" })
            </div>
            <div class="input-group">
                @Html.PasswordFor(m => m.Contrasena, new { @class = "form-control", @placeholder = "Contraseña" })
            </div>
            <div class="input-group">
                @Html.DropDownListFor(m => m.RolID, new SelectList(ViewBag.Roles, "RolID", "Nombre"), new { @class = "form-control" })
            </div>
            <div class="regLog-Submit mt-3">
                <button type="submit" class="btn btn-block">Registrarse</button>
            </div>
        }

        <p class="mt-4 mb-1 regLog-Options">
            <a href="@Url.Action("Iniciarsesion", "Home")">¿Ya tienes una cuenta? Ingresar</a>
        </p>
    </section>
</main>

<script>
    const body = document.getElementById("bodyRegLog");
    body.classList.add("bodyRegister")
</script>
